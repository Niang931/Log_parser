# DeepParse v2 — Python requirements
# Core
pandas>=2.0.0
pyyaml>=6.0
jsonschema>=4.0

# LLM providers (install what you have a key for)
anthropic>=0.34.0        # primary provider (claude-sonnet-4)
openai>=1.40.0           # optional
# groq>=0.9.0            # optional — ultra-low latency

# Observability / misc
python-dateutil>=2.8
